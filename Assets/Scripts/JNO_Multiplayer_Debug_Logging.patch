[1mdiff --git a/Assets/Scripts/CraftData/ReceiveCraftData.cs b/Assets/Scripts/CraftData/ReceiveCraftData.cs[m
[1mindex d8a937e..42bd15b 100644[m
[1m--- a/Assets/Scripts/CraftData/ReceiveCraftData.cs[m
[1m+++ b/Assets/Scripts/CraftData/ReceiveCraftData.cs[m
[36m@@ -17,6 +17,7 @@[m [mnamespace Assets.Scripts.Multiplayer.CraftData[m
             string trimmed = craftData.TrimStart();[m
             if (trimmed.StartsWith("<"))[m
             {[m
[32m+[m[32m                Mod.Log($"[ReceiveCraftData] Received raw craft XML ({craftData.Length} characters).");[m
                 return craftData;[m
             }[m
 [m
[36m@@ -27,7 +28,9 @@[m [mnamespace Assets.Scripts.Multiplayer.CraftData[m
                 using (GZipStream gzip = new GZipStream(input, CompressionMode.Decompress))[m
                 using (StreamReader reader = new StreamReader(gzip, Encoding.UTF8))[m
                 {[m
[31m-                    return reader.ReadToEnd();[m
[32m+[m[32m                    string xml = reader.ReadToEnd();[m
[32m+[m[32m                    Mod.Log($"[ReceiveCraftData] Decompressed craft XML from {compressedBytes.Length} bytes to {xml.Length} characters.");[m
[32m+[m[32m                    return xml;[m
                 }[m
             }[m
             catch (Exception ex)[m
[36m@@ -50,6 +53,7 @@[m [mnamespace Assets.Scripts.Multiplayer.CraftData[m
                 return Task.FromResult<CraftNode>(null);[m
             }[m
 [m
[32m+[m[32m            Mod.Log($"[ReceiveCraftData] Queued remote craft spawn for Client ID {clientId} ({decompressedXml.Length} XML characters).");[m
             return MultiplayerThread.Enqueue(() =>[m
                 CraftSpawner.SpawnCraftOnGameThread(clientId, decompressedXml));[m
         }[m
[1mdiff --git a/Assets/Scripts/Multiplayer/ClientConnection.cs b/Assets/Scripts/Multiplayer/ClientConnection.cs[m
[1mindex d784495..abe62ac 100644[m
[1m--- a/Assets/Scripts/Multiplayer/ClientConnection.cs[m
[1m+++ b/Assets/Scripts/Multiplayer/ClientConnection.cs[m
[36m@@ -30,6 +30,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
         {[m
             hostIp = host;[m
             TcpPort = port;[m
[32m+[m[32m            Mod.Log($"[ClientConnection] Beginning TCP CONNECT handshake to {host}:{port}.");[m
 [m
             if (IsConnected)[m
             {[m
[36m@@ -50,7 +51,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 _outboundWriter = new SerializedTcpWriter(client, "Host server");[m
                 _outboundWriter.Start();[m
 [m
[31m-                Mod.Log("[ClientConnection] Connected; waiting for host to accept conection.");[m
[32m+[m[32m                Mod.Log($"[ClientConnection] CONNECT frame sent; serialized outbound writer started for {client.Client.RemoteEndPoint}. Waiting for CONNECT_ACCEPTED.");[m
                 _ = StartListeningAsync(client);[m
             }[m
             else[m
[36m@@ -62,6 +63,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
         public static void Disconnect()[m
         {[m
             int localClientId = LocalClientId;[m
[32m+[m[32m            Mod.Log($"[ClientConnection] Beginning disconnect cleanup for local Client ID {localClientId}.");[m
             _initialCraftUploadPending = false;[m
 [m
             _telemetryUpdater?.Dispose();[m
[36m@@ -142,6 +144,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                     {[m
                         // XML parsing/decompression stays in this path. The actual Juno[m
                         // spawn is queued and completed by MultiplayerThread.Pump().[m
[32m+[m[32m                        Mod.Log($"[ClientConnection] Received SPAWN_CRAFT for remote Client ID {remoteClientId} ({(data == null ? 0 : data.Length)} characters); queueing game-thread spawn.");[m
                         await ReceiveCraftData.ProcessAndSpawnAsync(remoteClientId, data);[m
                     }[m
 [m
[36m@@ -160,6 +163,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
 [m
                         LocalClientId = assignedId;[m
                         _initialCraftUploadPending = true;[m
[32m+[m[32m                        Mod.Log($"[ClientConnection] Received CONNECT_ACCEPTED; assigned Client ID {assignedId}. Waiting for flight-ready local craft XML.");[m
                         StartTelemetryAfterHandshake();[m
                         break;[m
 [m
[36m@@ -211,6 +215,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
             }[m
 [m
             _initialCraftUploadPending = false;[m
[32m+[m[32m            Mod.Log($"[ClientConnection] Captured local craft XML ({compressedXml.Length} characters); queueing CLIENT_CRAFT_DATA.");[m
             _ = SendInitialCraftXmlAsync(client, compressedXml);[m
         }[m
 [m
[36m@@ -228,6 +233,10 @@[m [mnamespace Assets.Scripts.Multiplayer[m
             {[m
                 Mod.LogWarning("[ClientConnection] Could not queue initial craft XML.");[m
             }[m
[32m+[m[32m            else[m
[32m+[m[32m            {[m
[32m+[m[32m                Mod.Log("[ClientConnection] CLIENT_CRAFT_DATA queued successfully.");[m
[32m+[m[32m            }[m
 [m
             return Task.CompletedTask;[m
         }[m
[1mdiff --git a/Assets/Scripts/Multiplayer/ServerConnection.cs b/Assets/Scripts/Multiplayer/ServerConnection.cs[m
[1mindex 4f68967..04d3fb5 100644[m
[1m--- a/Assets/Scripts/Multiplayer/ServerConnection.cs[m
[1m+++ b/Assets/Scripts/Multiplayer/ServerConnection.cs[m
[36m@@ -71,6 +71,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 return;[m
             }[m
 [m
[32m+[m[32m            Mod.Log($"[ServerHost] Starting host setup on TCP/UDP port {port}.");[m
             await PortForwarder.ForwardPort(port);[m
 [m
             try[m
[36m@@ -103,6 +104,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 while (_isHosting)[m
                 {[m
                     TcpClient client = await _listener.AcceptTcpClientAsync();[m
[32m+[m[32m                    Mod.Log($"[ServerHost] Accepted TCP peer {client.Client.RemoteEndPoint}; awaiting CONNECT handshake.");[m
                     _ = HandleClientAsync(client);[m
                 }[m
             }[m
[36m@@ -228,6 +230,8 @@[m [mnamespace Assets.Scripts.Multiplayer[m
         #region TCP receive loop[m
         private static async Task HandleClientAsync(TcpClient client)[m
         {[m
[32m+[m[32m            Mod.Log($"[ServerHost] TCP receive loop started for {client.Client.RemoteEndPoint}.");[m
[32m+[m
             try[m
             {[m
                 using (var receiver = new TcpNetworkReceiver())[m
[36m@@ -268,6 +272,8 @@[m [mnamespace Assets.Scripts.Multiplayer[m
 [m
                 if (clientId != -1)[m
                 {[m
[32m+[m[32m                    Mod.Log($"[ServerHost] Client ID {clientId} disconnected; removing session, telemetry endpoint, and proxy.");[m
[32m+[m
                     lock (Sessions)[m
                     {[m
                         Sessions.Remove(departedSession);[m
[36m@@ -278,6 +284,10 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                     MultiplayerThread.Post(() => CraftRegistry.DespawnCraft(clientId));[m
                     SendDataToClients(clientId.ToString(), "CLIENT_DISCONNECTED");[m
                 }[m
[32m+[m[32m                else[m
[32m+[m[32m                {[m
[32m+[m[32m                    Mod.Log($"[ServerHost] TCP peer {client.Client.RemoteEndPoint} disconnected before completing CONNECT.");[m
[32m+[m[32m                }[m
             }[m
         }[m
 [m
[36m@@ -301,6 +311,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
 [m
             // Start the one and only code path allowed to write to this client's TCP stream.[m
             session.StartWriter();[m
[32m+[m[32m            Mod.Log($"[ServerHost] Created session for Client ID {session.Id} from {client.Client.RemoteEndPoint}.");[m
 [m
             EnsureHostTelemetryStarted();[m
 [m
[36m@@ -344,6 +355,8 @@[m [mnamespace Assets.Scripts.Multiplayer[m
             int clientId = GetClientId(client);[m
             if (clientId == -1) return;[m
 [m
[32m+[m[32m            Mod.Log($"[ServerHost] Received CLIENT_CRAFT_DATA from Client ID {clientId} ({(data == null ? 0 : data.Length)} characters).");[m
[32m+[m
             lock (Sessions)[m
             {[m
                 ClientSession session = Sessions.Find(s => s.Id == clientId);[m
[36m@@ -361,6 +374,7 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 return;[m
             }[m
 [m
[32m+[m[32m            Mod.Log($"[ServerHost] Remote proxy is ready for Client ID {clientId}; waiting for host craft XML if needed.");[m
             string hostXml = await WaitForHostCraftXmlAsync();[m
             if (!_isHosting || !client.Connected || string.IsNullOrEmpty(hostXml))[m
             {[m
[36m@@ -379,13 +393,21 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 }[m
             }[m
 [m
[32m+[m[32m            ClientSession joiningSession = FindSession(client);[m
[32m+[m[32m            if (joiningSession == null)[m
[32m+[m[32m            {[m
[32m+[m[32m                Mod.LogWarning($"[ServerHost] Client ID {clientId} disappeared before existing craft XML could be queued.");[m
[32m+[m[32m                return;[m
[32m+[m[32m            }[m
[32m+[m
[32m+[m[32m            Mod.Log($"[ServerHost] Sending {existingCrafts.Count} existing craft definition(s) to joining Client ID {clientId}.");[m
             foreach (KeyValuePair<int, string> existingCraft in existingCrafts)[m
             {[m
[31m-                var _client = FindSession(client);[m
[31m-                _client.EnqueuePacket(existingCraft.Value, $"SPAWN_CRAFT:{existingCraft.Key}");[m
[32m+[m[32m                joiningSession.EnqueuePacket(existingCraft.Value, $"SPAWN_CRAFT:{existingCraft.Key}");[m
             }[m
 [m
             SendDataToClients(data, $"SPAWN_CRAFT:{clientId}", excludeClientId: clientId);[m
[32m+[m[32m            Mod.Log($"[ServerHost] Broadcast new craft definition for Client ID {clientId} to established peers.");[m
         }[m
         #endregion[m
 [m
[36m@@ -403,6 +425,8 @@[m [mnamespace Assets.Scripts.Multiplayer[m
                 }[m
             }[m
 [m
[32m+[m[32m            Mod.Log($"[ServerHost] Broadcasting '{metadata}' to {destinations.Count} client session(s); excluding Client ID {excludeClientId}.");[m
[32m+[m
             foreach (ClientSession session in destinations)[m
             {[m
                 if (!session.EnqueuePacket(data, metadata)) Mod.LogWarning($"[ServerHost] Could not queue '{metadata}' for Client ID {session.Id}.");[m
[1mdiff --git a/Assets/Scripts/Threading/SerializedTcpWriter.cs b/Assets/Scripts/Threading/SerializedTcpWriter.cs[m
[1mindex 6fef8b4..86bb038 100644[m
[1m--- a/Assets/Scripts/Threading/SerializedTcpWriter.cs[m
[1m+++ b/Assets/Scripts/Threading/SerializedTcpWriter.cs[m
[36m@@ -33,7 +33,9 @@[m [mnamespace Assets.Scripts.Threading[m
         public void Start()[m
         {[m
             if (_closed || _writerTask != null) return;[m
[32m+[m
             _writerTask = WriteLoopAsync();[m
[32m+[m[32m            Mod.Log($"[TCP] Serialized writer started for {_ownerName}.");[m
         }[m
 [m
         public bool Enqueue(string data, string metadata)[m
[36m@@ -43,9 +45,13 @@[m [mnamespace Assets.Scripts.Threading[m
                 return false;[m
             }[m
 [m
[31m-            _outboundFrames.Enqueue([m
[31m-                NetworkSender.BuildPacket(data ?? string.Empty, metadata));[m
[32m+[m[32m            byte[] frame = NetworkSender.BuildPacket(data ?? string.Empty, metadata);[m
[32m+[m[32m            _outboundFrames.Enqueue(frame);[m
             _outboundSignal.Release();[m
[32m+[m
[32m+[m[32m            // TCP carries infrequent handshake, craft XML, and control messages only.[m
[32m+[m[32m            // Logging this boundary is useful without becoming UDP telemetry spam.[m
[32m+[m[32m            Mod.Log($"[TCP] Queued '{metadata}' ({frame.Length} bytes) for {_ownerName}.");[m
             return true;[m
         }[m
 [m
[36m@@ -54,6 +60,7 @@[m [mnamespace Assets.Scripts.Threading[m
             try[m
             {[m
                 NetworkStream stream = _client.GetStream();[m
[32m+[m[32m                Mod.Log($"[TCP] Writer loop entered for {_ownerName}.");[m
 [m
                 while (!_cancellation.IsCancellationRequested)[m
                 {[m
[36m@@ -74,6 +81,10 @@[m [mnamespace Assets.Scripts.Threading[m
             {[m
                 Mod.LogWarning($"[TCP] Writer for {_ownerName} stopped: {ex.Message}");[m
             }[m
[32m+[m[32m            finally[m
[32m+[m[32m            {[m
[32m+[m[32m                Mod.Log($"[TCP] Writer loop exited for {_ownerName}.");[m
[32m+[m[32m            }[m
         }[m
 [m
         public void Dispose()[m
[36m@@ -81,6 +92,8 @@[m [mnamespace Assets.Scripts.Threading[m
             if (_closed) return;[m
             _closed = true;[m
 [m
[32m+[m[32m            Mod.Log($"[TCP] Disposing writer for {_ownerName}; abandoning approximately {_outboundFrames.Count} queued frame(s).");[m
[32m+[m
             _cancellation.Cancel();[m
             _outboundSignal.Release();[m
 [m
